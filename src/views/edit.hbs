<div class="conainer">
    <div class="row">
        <div class="col-md-5 offset-md-3">
            <div class="card card-body bg-purple p-4 text-withe rounder-10">
                <h1 class="text-white">Update a task</h1>
                <form action="/edit/{{task._id}}" method="post">
                
                    <input 
                        type="text" 
                        name="title" 
                        placeholder="Title" 
                        value="{{task.title}}" 
                        class="form-control rounder-10 bg-purpleclear text-white"
                    ><br>

                    <textarea 
                        name = "description" 
                        rows = "3" 
                        placeholder = "Add a Description"
                        class="form-control rounder-10 bg-purpleclear text-white"
                    >{{task.description}}</textarea><br>

                    <button  class="btn btn-purpledark text-white"> Update </button>
                </form>
            </div>
        </div>
    </div>
</div>