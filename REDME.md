### Envirommrnt variables
Para que funcione de manera correcta se tienen que agregar un archivo llamado .env con las siguietes variables 

MONGODB_URI = "la direccion con la conexion de tu base de datos"
PORT = 5000